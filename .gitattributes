# The bundled engine is generated: keep it out of textual diffs so the
# reproducibility gate (check:build) compares bytes, not rendered patches.
# (The tree-sitter grammar wasms are no longer committed — the engine pulls
# them into a shared per-machine cache at skill-use time.)
scripts/ultraindex.mjs -diff
skills/ultraindex/scripts/ultraindex.mjs -diff
