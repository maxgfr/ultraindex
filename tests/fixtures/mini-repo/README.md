---
title: Mini Repo
---

# Mini Repo

A tiny polyglot fixture used to exercise ultraindex. The HTTP layer is built
around the `HttpClient` class, which uses exponential `backoff` on retry.

See the [user guide](docs/guide.md) and the [API reference](docs/api.md).
